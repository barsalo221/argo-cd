apiVersion: v1
kind: Service
metadata:
  name: {{ include "car-app.fullname" . }}-svc
  labels: {{- include "car-app.labels" . | nindent 4 }}
  annotations:
spec:
  selector:
    {{- include "car-app.selectorLabels" . | nindent 4 }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
