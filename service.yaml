apiVersion: v1
kind: Service
metadata:
  name: my-car-service
spec:
  type: NodePort     
  selector:
    app: web          
  ports:
    - protocol: TCP
      port: 80        
      targetPort: 5000 
      nodePort: 30001